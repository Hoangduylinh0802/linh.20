@model IEnumerable<QLPHANMEM.Models.HoSo>

@{
    ViewBag.Title = "Index";
}

<h2>Thông Tin Hồ Sơ</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TENHS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayLapHS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SLHS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhanVien.HOTEN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENHS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayLapHS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SLHS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhanVien.HOTEN)
        </td>
        <td>
            @Html.ActionLink("Thêm", "Edit", new { id=item.MSHS }) |
            @Html.ActionLink("Sửa", "Details", new { id=item.MSHS }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MSHS })
        </td>
    </tr>
}

</table>
